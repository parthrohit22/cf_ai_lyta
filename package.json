{
	"name": "cf-ai-lyta",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build:client": "tsc -p tsconfig.client.json",
		"check": "npm run build:client && npm test && tsc --noEmit && tsc -p tsconfig.client.json --noEmit && node --check pages/app-core.js && node --check pages/app.js && node --check pages/app-attachments.js && git diff --exit-code -- pages/app-core.js && npm audit --omit=dev --audit-level=high",
		"deploy": "npm run build:client && wrangler deploy",
		"test": "npm run build:client && tsx --test tests/**/*.test.ts",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20260312.1",
		"@types/node": "^25.2.3",
		"tsx": "^4.21.0",
		"typescript": "^5.5.2",
		"wrangler": "^4.64.0"
	}
}
